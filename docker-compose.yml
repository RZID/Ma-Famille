services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: mafamille
      POSTGRES_PASSWORD: mafamille
      POSTGRES_DB: mafamille
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mafamille -d mafamille"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
