# Tunnel hostname reference for ma-famille.
#
# This tunnel runs in TOKEN mode (--token-file), so there is NO local
# config.yml: hostnames live in the dashboard under
# Zero Trust → Networks → Tunnels → <tunnel> → Public Hostnames.
# Add exactly these two rows (Service hosts are backend-VM LAN IPs):
#
#   be-ssh.rzidinc.com      →  ssh://10.10.0.30:22
#   college.rzidinc.com
#     path /ma-famille/*    →  http://10.10.0.30:8080
#
# Protect be-ssh.rzidinc.com with a Zero Trust Access app so only the
# deploy service token can open SSH (Dashboard → Access → Applications).
# Changes apply immediately; nothing restarts.
